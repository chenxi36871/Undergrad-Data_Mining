1. 写出或推导贝叶斯公式，分别包含集合情形，随机变量的情形（含离散和连续四类情形）

2. 对LDA模拟仿真生成的数据，使用QDA和朴素贝叶斯方法进行分析，画出决策边界

3 对玩具数据$(X,Y):
\{(-3,0),(-2,0),(-1,0),(1,1),(2,1),(3,1)\}$，使用带有和不带有L2惩罚项的逻辑回归来估计参数，并观察结果。

4. 写出带L2惩罚的逻辑回归的Python程序。（可选做）

1. Write or derive the Bayesian formula, including the case of sets and the case of random variables (including discrete and continuous cases)

2. Analyze the data generated by the LDA simulation using QDA and Naive Bayes methods to draw decision boundaries

3. For the data $(X,Y):
\{(-3,0),(-2,0),(-1,0),(1,1),(2,1),(3,1)\}$, Estimate the parameters using logistic regression with and without an L2 penalty term, and observe the results.

4. Write a Python program for logistic regression with L2 penalty. (optional)